runtime: "container"
startCommand:
  type: "stdio"
  command: "node"
  args: ["index.js"]
description: "Real-time email validation: MX records, SMTP delivery, disposable domains, typo detection. Built for lead-qual and outreach agents."
